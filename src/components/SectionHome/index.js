import { Box } from '@chakra-ui/layout';
import React from 'react';
import { SectionDescription, SectionTitle } from '../TextElements';

const SectionHome = () => {
    return (
        <Box as='section' className='section' h='100vh'>

        </Box>
    );
};

export default SectionHome;